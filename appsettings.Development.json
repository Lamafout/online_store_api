{
  "DbSettings": {
    "MigrationConnectionString": "postgres://${DB_USER}:${DB_PASSWORD}@127.0.0.1:5432/${DB_NAME}?sslmode=disable",
    "ConnectionString": "postgres://${DB_USER}:${DB_PASSWORD}@127.0.0.1:15432/${DB_NAME}?sslmode=disable"
  }
}